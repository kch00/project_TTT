<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.profiletest2">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="TTT"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ProfileTest2"
        tools:replace="android:label,android:icon,android:theme"
        tools:targetApi="31">

        <activity android:name=".SplashActivity"
            android:exported="true"
            tools:ignore="IntentFilterExportedReceiver">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".ProfileEditActivity" android:label="TTT" />
        <activity android:name=".MainActivity" android:exported="true" android:label="TTT">
            <meta-data android:name="android.app.lib_name" android:value="" />
        </activity>
        <activity android:name=".TodoListActivity" android:label="TTT" />
        <activity android:name=".AddTodoActivity" android:label="TTT" />
        <activity android:name=".CheckInOutActivity" android:label="TTT" />
        <activity android:name=".LoginActivity" android:label="TTT" />
        <activity android:name=".RegisterActivity" android:label="TTT" />
    </application>

</manifest>
